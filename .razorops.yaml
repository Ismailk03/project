tasks:
  build:
    steps:
    - checkout
    # commands to build Sparlex – Beauty & Spa Website Template
    - commands:
    - gem install bundler
      - rm Gemfile.lock
      - bundle install
      - JEKYLL_ENV=production bundle exec jekyll build

      
    # commands to upload static pages folder to AWS S3
    - commands:
      - aws s3 rm s3://$project-0001 --recursive
      - aws s3 cp _site s3://$project-0001 --recursive
      if: branch == 'master'
