tasks:
  build:
    steps:
      - checkout
       # ... other build steps   
      # commands to upload static pages folder to AWS S3
      - commands:
          - aws configure set aws_access_key_id $AKIAUEEQUF2II435G5IN
          - aws configure set aws_secret_access_key $oQ+sPuBvXAQ+MEpWk7vNc6F+Avy36eWRMi/XFAX8
          - aws s3 rm s3://$project-0001 --recursive
          - aws s3 sync _site/ s3://http://project-0001.s3-website-us-east-1.amazonaws.com--delete

    if: branch == 'master'
